# This .env file is only required for CLI operations:
# - Schema sync (npm run directus:pull / directus:push)
# - Database backup/restore (npm run db:backup / db:restore)

# Directus admin token for CLI operations
# Generate a static token in Directus admin panel under Settings > Access Tokens
DIRECTUS_ADMIN_TOKEN="your-admin-token-here"

# Public URLs (used by docker-compose)
FRONTEND_PUBLIC_URL="http://localhost:3000"
API_PUBLIC_URL="http://localhost:8055"

# Port bindings (optional - defaults to random ports)
# Set these for production/staging to configure reverse proxies
# Leave commented for development (random ports will be assigned)
# API_PORT=8055
# FRONTEND_PORT=3000
